<div class="card">
  <div class="card-image">
    <figure class="image is-256x256">
      <img src={{collection.imageUrl}}>
    </figure>
  </div>
  <div class="card-content">
    <form action="/collection/{{collection._id}}/uploadImage" method="POST" enctype="multipart/form-data">
      <div id="file-select" class="file has-name is-fullwidth">
        <label class="file-label"> <input class="file-input" name="imageFile" type="file" accept="image/png, image/jpeg">
          <span class="file-cta">
            <span class="file-icon">
              <i class="fas fa-upload"></i>
            </span>
            <span class="file-label">
              Choose a file…
            </span>
           </span>
          <span class="file-name"></span>
        </label>
        <button type="submit" class="button is-primary">Upload</button>
        {{#if collection.imageUrl}}
          <a id="delete" class="button is-danger" href="/collection/{{collection._id}}/deleteImage">Delete</a>
        {{/if}}
      </div>
    </form>
  </div>
</div>

<script>
  const fileInput = document.querySelector(".file-input");
  fileInput.onchange = () => {
    if (fileInput.files.length > 0) {
      const fileName = document.querySelector(".file-name");
      fileName.textContent = fileInput.files[0].name;
    }
  };
</script>